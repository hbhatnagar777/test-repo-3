prerequisite:
    Python should be installed
    user should have backup and restore access and should be a workflow admin

steps to run
    Fill the restore config in the restoreconfig.json
    Run the run_vmdk_restore.bat in the folder
    Logs will be printed in vmdkrestore.log